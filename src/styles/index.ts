import { headerCenterElement } from './mixins';
import {
  getTopSafeArea,
  getBottomSafeArea,
  calcTopPadding,
  calcBottomPadding,
} from './utils';

const mixins = {
  headerCenterElement,
};

const utils = {
  getTopSafeArea,
  getBottomSafeArea,
  calcTopPadding,
  calcBottomPadding,
};

export { mixins, utils };
